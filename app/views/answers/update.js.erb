$('#<%= dom_id(@answer.question) %>').replaceWith("<%= escape_javascript(render :partial => 'assessments/assessment_question', :object => @answer, :as => :answer) %>");

// Need to find it again because it's been replaced:
$('#<%= dom_id(@answer.question) %>').
  find('.save-response').attr('disabled', true).end().
  find('.info-bubble').each(manageInfoBubble);

$('.progress-container').replaceWith("<%= escape_javascript(render :partial => 'assessments/progress') %>");
$('.critical-function-toggle').replaceWith("<%= escape_javascript(render :partial => 'assessments/critical_function_toggle') %>");

