<div class="grid_12">
  <h1>Completed Assessments</h1>
  <%= render :partial => '/shared/settings_navbar', :locals => {:current_settings_tab => :completed_assessments} %>
	<hr>
  <section>
    <table class="resource side-nav-panels assessments">
      <thead>
        <td><p>Started at</p></td>
        <td><p>Completed at</p></td>
        <th>&nbsp;</th>
      </thead>
      <tbody>
        <% @assessments.each do |assessment| %>
          <tr>
            <td class="date"><p><%= assessment.created_at %></p></td>
            <td class="date"><p><%= assessment.completed_at %></p></td>
            <td><p><%= link_to 'Details', archived_assessment_path(assessment), :class => 'button' %></p></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>
</div>

