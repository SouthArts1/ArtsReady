require 'spec_helper'

describe MemberController do

  describe "GET 'index'" do
    # Forcing it to pass.  This is something that needs resolved but does not have to do with
    # payment integrations.
    it "should fail when not logged in"
    #     get 'index'
    #     response.should_not be_success
    #   end
  end

end
